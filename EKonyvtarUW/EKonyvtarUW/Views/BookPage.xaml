<Page
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    xmlns:Common="using:EKonyvtarUW.Common"
    x:Class="EKonyvtarUW.Views.BookPage"
    mc:Ignorable="d"
    >

    <Page.Resources>
        <Common:FavoriteColorConverter x:Key="FavoriteColorConverter"/>
    </Page.Resources>

    <Grid Background="#33808080" >
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"></RowDefinition>
            <RowDefinition></RowDefinition>
        </Grid.RowDefinitions>

        <CommandBar Grid.Row="0">
            <CommandBar.Content>
                <TextBlock Text="{Binding book.Title}"  Style="{StaticResource AppTitleTextBlockStyle}" TextWrapping="NoWrap" />
            </CommandBar.Content>
            <CommandBar.SecondaryCommands>
                <AppBarButton Label="Olvasás" Icon="Read" Click="ReadButton_Click"/>
                <AppBarButton Label="Kedvenc" Icon="Favorite" Click="FavoriteButton_Click" Foreground="{Binding book.IsFavorite, Converter={StaticResource FavoriteColorConverter}}"/>
                <AppBarButton Label="Letöltés" Icon="Download" Click="SaveButton_Click"/>
                <AppBarButton Label="Megosztás" Icon="ReShare" Click="Share_Click"/>
            </CommandBar.SecondaryCommands>
        </CommandBar>
        <ProgressBar IsDoubleTapEnabled="False" IsHoldingEnabled="False" IsRightTapEnabled="False" IsTapEnabled="False" IsIndeterminate="{Binding IsLoading}" Minimum="0" Maximum="100" Grid.Row="0" VerticalAlignment="Bottom"/>
        <Hub Grid.Row="1">
            <HubSection Width="230">
                <DataTemplate>
                    <StackPanel Width="200">
                        <Image
                            Source="{Binding book.ThumbnailUrl}"
                            Height="224"
                            Width="182"
                            HorizontalAlignment="Center"
                            Stretch="UniformToFill"
                        />

                        <StackPanel Orientation="Horizontal" HorizontalAlignment="Center">
                            <AppBarButton Label="Kedvenc" Icon="Favorite" Click="FavoriteButton_Click" Foreground="{Binding book.IsFavorite, Converter={StaticResource FavoriteColorConverter}}"/>
                            <AppBarButton Label="Letöltés" Icon="Download" Click="SaveButton_Click"/>
                            <AppBarButton Label="Megosztás" Icon="ReShare" Click="Share_Click"/>
                        </StackPanel>
                        <ComboBox x:Name="SourceList" ItemsSource="{Binding book.MediaDictionary}"  Margin="8"  Width="180" HorizontalAlignment="Center" DisplayMemberPath="Key" IsEnabled="{Binding IsReady}" IsEnabledChanged="FileType_IsEnabledChanged" SelectionChanged="FileType_SelectionChanged" Visibility="Collapsed"/>
                        <Button Content="Olvasás"  Margin="0,4,0,4"  HorizontalAlignment="Center" Width="180" Click="ReadButton_Click" IsEnabled="{Binding IsReady}"/>
                        <TextBlock Text="{Binding book.Abbreviation}" TextWrapping="Wrap" FontSize="10" Width="200" Margin="10,0,0,0"/>
                    </StackPanel>
                </DataTemplate>
            </HubSection>
            <HubSection Header="Kivonat" Margin="10,0,0,0" Width="460">
                <DataTemplate>
                    <StackPanel>
                        <!--<TextBlock Text="{Binding book.Contents}" TextWrapping="Wrap" FontSize="12" TextAlignment="Justify"></TextBlock>-->
                        <TextBlock Text="{Binding book.Summary}" TextWrapping="Wrap" FontSize="12" TextAlignment="Justify"/>
                    </StackPanel>
                </DataTemplate>
            </HubSection>
            <HubSection Header="Ajánló" Margin="10,0,0,0" Width="320">
                <DataTemplate>
                    <TextBlock Text="{Binding book.Recommendation}" TextWrapping="Wrap" FontSize="12" TextAlignment="Justify"/>
                </DataTemplate>
            </HubSection>
            <HubSection Header="Kapcsolódó könyvek" Margin="20,0,0,0" Width="500" Visibility="Collapsed">
                <DataTemplate>
                    <ScrollViewer Style="{StaticResource PageScrollViewerStyle}">
                        <StackPanel>
                            <GridView ItemsSource="{Binding MekFeed.Result}" IsItemClickEnabled="True" >
                                <GridView.ItemTemplate>
                                    <DataTemplate>
                                        <StackPanel VerticalAlignment="Top" Orientation="Vertical">
                                            <TextBlock Text="{Binding Title}" />
                                            <Image Source="{Binding ImageUrl}" Height="160" Width="130"/>
                                            <TextBlock Text="{Binding Text}" />
                                        </StackPanel>
                                    </DataTemplate>
                                </GridView.ItemTemplate>
                            </GridView>
                            <!-- 
                            <TextBlock Text="{Binding HelloWorld}"></TextBlock>
                            <TextBlock Text="Happy coding!" />

                            <Button Content="Goto Page1" Command="{Binding GotoPage1Command}"></Button>
                            -->
                        </StackPanel>
                    </ScrollViewer>
                </DataTemplate>
            </HubSection>
        </Hub>

    </Grid>
</Page>
